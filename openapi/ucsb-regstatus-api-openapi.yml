openapi: 3.2.0
info:
  version: v1.6
  title: Student Record Code Lookups Reg Status API
  description: Code Lookups from the Office of the Registrar.
servers:
- url: https://api.ucsb.edu/students
tags:
- name: RegStatus
paths:
  /lookups/v1/regstatus:
    get:
      tags:
      - RegStatus
      operationId: /lookups/v1/regstatus
      parameters:
      - name: includeInactive
        in: query
        required: false
        schema:
          type: boolean
          default: false
      - name: ucsb-api-version
        in: header
        description: Specific api version to use.
        required: false
        schema:
          type: string
          default: '1.6'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AcademicRegStatus'
            text/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AcademicRegStatus'
      description: Gets the list of school type codes.
      security:
      - ucsb-api-key: []
      summary: /lookups/v1/regstatus
      x-summary-source: derived
components:
  schemas:
    AcademicRegStatus:
      type: object
      properties:
        regStatusCode:
          type: string
        regStatusTranslation:
          type: string
        comments:
          type: string
        inactive:
          type: boolean
  securitySchemes:
    ucsb-api-key:
      name: ucsb-api-key
      in: header
      type: apiKey
